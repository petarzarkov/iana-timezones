Run all CI checks for this repo in order. Stop and report on the first failure.

1. **Lint** — `bun run lint`
2. **Typecheck** — `bun run typecheck`
3. **Test** — `bun run test`
4. **Build** — `bun run build`

Run each command using the Bash tool. After all pass, confirm with a brief summary of results.
